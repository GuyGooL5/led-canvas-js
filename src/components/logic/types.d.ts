/**
 * @param number - between 0 and 1
 * @param string - string color of hex or rgba or hsl
 */
type ColorStop = [number, string];

type Dimensions = [w: number, h: number];

type Coords = [x: number, y: number];